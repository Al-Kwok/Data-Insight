# Project Requirements

- You cannot use any library package that is not already specified in package.json or required for TypeScript compilation (i.e.: types)
- Your implementation must be in TypeScript
- You are not allowed to store data in any external database, only disk storage is permitted
- Do not store datasets as static or global variables; keep them as members of a class to ensure proper test isolation